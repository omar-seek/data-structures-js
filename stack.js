// Stack: LIFO (Last In First Out)
//  - The last element added to the stack will be the first element removed from the stacks
// push, pop
// Stack Usage.
// - Browser history tracking (the back button)
// - command-z (undo) in a text editor
// Expression conversions (infix to postfix, etc) like `2 + 3 -> 2 3 +` & `2 3 + -> 2 + 3`
// Call stack in JavaScript (function calls) example below:
// function foo() {
//   return foo();
// }
